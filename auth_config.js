// auth_config.js

// Your Auth0 tenant domain name
// and the client ID are public information.
const config = {
  domain: 'YOUR_AUTH0_DOMAIN',
  clientId: 'YOUR_APP_CLIENT_ID',
};

export default config;
